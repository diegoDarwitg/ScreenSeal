[package]
name = "screenseal-core"
version = "0.1.0"
edition = "2024"

[lib]
name = "screenseal_core"
path = "src/lib.rs"

[dependencies]
serde = { version = "1", features = ["derive"] }
bincode = { version = "2", features = ["serde"] }